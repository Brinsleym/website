# Development overrides. Hugo applies these automatically for `hugo server`
# (and any command run with --environment development).
baseURL = "http://localhost:1313"

[params]
# Serve CSS/JS/images locally instead of from the CDN
cdn_domain = ""

# Language params beat [params], so zh needs its own override
[languages.zh.params]
cdn_domain = ""
